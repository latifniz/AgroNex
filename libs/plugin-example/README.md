# plugin-example

Reference plugin implementation for the AgroNex platform. Demonstrates the standard Vendure plugin structure: entity, resolver, GraphQL schema extension, and dashboard UI extension.

Use this as a starting point when scaffolding a new plugin, or run the generator instead:

```bash
nx g vendure-nx:vendure-plugin-generator --name=MyPlugin --uiExtension=true
```
