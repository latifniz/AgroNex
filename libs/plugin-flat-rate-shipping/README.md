# plugin-flat-rate-shipping

Vendure plugin providing per-channel flat-rate shipping methods with configurable eligibility checkers. Used to set different shipping rates for the retail and wholesale channels on AgroNex.

Registered in `libs/util-config`. Configure shipping methods per channel in the admin dashboard under **Shipping → Shipping Methods**.
